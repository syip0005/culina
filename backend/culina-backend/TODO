- Incorporate AFCD information, with search functionality. [Done]
- Find general alternatives if AFCD information isn't enough [Done] (just use lookup)
- Passive macro understanding from photos, predictions? (based on previous eating history, time of day)
- Macro trajectory prediction [Done]
- Design for database for users, cached products (Redis?) [Done]
- Nutrition Service Layer [Done]
- Solve for serving sizes (must be solved before MVP) [Done]
- Row level security (RLS)
- Photo service
- Initial Routes [Done]
- Multi-item routes / batch routes
- Auth [Done]
- Evals for AI
- Redis layer for conversation history so we can scale to multiprocess

Bugs

- For volume / weight, picker should go up and down by 1ml or 1grm, but for serving, just go by increments of 0.5. [Done]
- Embedding searches always appear even if irrelevant, we need to set a better threshold [Done]
- Search bar doesn't feel fluid, it's either the debounce or something, but there has to be a UX trick here. [Done]
- After deleting an item, we can use the current trick to delete quicker, but we do eventually need to update the summary totals. [Done]
- In the backend, we don't handle when we go beyodn 5 tool uses. It just fails to fetch and the error is not being propogated. [Done]
- When I select KCal in settings, this only updates the summary total at the top, but only the number. In remaining, neither number nor unit is updated, nor in any of the other categories. [Done]

Improvements

- Current colour scheme and layout is great, just need a better font. It's pushing too much brutalist. [Done]
- Back and forth buttons to go back and forth. Eager load in previous 2 days. [Done]
- We need to speed up the thinking of the AI. [Done] no good soln, done for now. maybe better or cheaper models later.
- [Backend] Introduce suggestions for "Add Item". Right now, it's just empty. based on top items first, or maybe what they ate yesterday or same time last week and recommender system later. It should probably be conditioned on `meal_type`. Create service layer / interchangable extensbility for this. What are the best ways here? What ML techniques work? [Frontend] Then update frontend for this to autopopulate list when they click add item. [Done]
- When showing irregular quantities, we just have the amount x whatever the serving size is. For UX, if these are weight items or ml items, they should be shown in those figures. [Done]
- On the main screen, highlight the + ADD ITEM depending on the time of day. [Done]
- Remove 'thinking' and make it something else. [Done]
- Reduce TTL to 60 seconds, and increase max concurrent to 5. [Done]
- Summary statistics for week / fortnight / month/ year, but these need to be quick, so maybe when user logins we also recalculate this I don't know what's the quickest. Precomputed? Or is it already indexed so its really quick? i'D probably also want graphs, produce statistics etc with this. [Done]
- Info button needs to be shown. [Done]
- Make the thinking slower like the animation [Done]
- Allow item additional information [Done]
- Allow modification of menu items, i.e., serving size. [Done]
- Allow modification of existing items [Done]
- Allow modification of the item being added [Done]
- Allow creating new item manually [Done]

Pre-alpha go live

- Logs / emitting logs [Done]
- Security audit [Done]
- Code quality audit [Done]

- Write updated docs [Done]

After

- Tests
- Better mobile UX and touching, especially say for info/delete buttons
- Move all our cached stuff to Redis, there's heaps now, inc. conversations, suggestions and somethigng else.
- On the UI, we need to allow image search and probably need some sort of S3 bucket at htat point.
- 429 a user if they make more than X amount of calls a day. Might have to track this in redis as well. Want to avoid 3rd party observability.
- Validate nutrition entry values (non-negative energy_kj, protein_g, fat_g, carbs_g; reasonable upper bounds)