- Incorporate AFCD information, with search functionality. [Done]
- Find general alternatives if AFCD information isn't enough [Done] (just use lookup)
- Passive macro understanding from photos, predictions? (based on previous eating history, time of day)
- Macro trajectory prediction [Done]
- Design for database for users, cached products (Redis?) [Done]
- Nutrition Service Layer [Done]
- Solve for serving sizes (must be solved before MVP) [Done]
- Row level security (RLS)
- Photo service
- Initial Routes [Done]
- Multi-item routes / batch routes
- Auth [Done]
- Evals for AI

Bugs

- For volume / weight, picker should go up and down by 1ml or 1grm, but for serving, just go by increments of 0.5. [Done]
- Embedding searches always appear even if irrelevant, we need to set a better threshold [Done]
- Search bar doesn't feel fluid, it's either the debounce or something, but there has to be a UX trick here. [Done]
- After deleting an item, we can use the current trick to delete quicker, but we do eventually need to update the summary totals. [Done]
- In the backend, we don't handle when we go beyodn 5 tool uses. It just fails to fetch and the error is not being propogated. [Done]
- When I select KCal in settings, this only updates the summary total at the top, but only the number. In remaining, neither number nor unit is updated, nor in any of the other categories. [Done]

Improvements

- Current colour scheme and layout is great, just need a better font. It's pushing too much brutalist. [Done]
- Back and forth buttons to go back and forth. Eager load in previous 2 days.
- We need to speed up the thinking of the AI.
- Introduce suggestions for "Add Item", based on top items first and recommender system later. Create service layer / interchangable extensbility for this. What are the best ways here? What ML techniques work?
- When showing irregular quantities, we just have the amount x whatever the serving size is. For UX, if these are weight items or ml items, they should be shown in those figures.
- Reduce TTL to 60 seconds, and increase max concurrent to 5.